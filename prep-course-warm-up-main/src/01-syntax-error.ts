export {};

// function addNumbers(a: number, b: number, c: number):number {
//   return a + b + c;
// }

const addNumbers = (a: number, b: number, c: number): number => a + b + c;

const result: number = addNumbers(1, 3, 4);
console.log(result); // Expected output: 8
